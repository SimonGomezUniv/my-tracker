// Fichier statique utilisé par GitHub Pages (production).
// En développement local, ce fichier est ignoré : le serveur Express
// sert /env.js dynamiquement avant le middleware static.
window.__ENV__ = { NODE_ENV: 'production', PORT: '' };

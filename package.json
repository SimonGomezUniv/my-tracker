{
  "name": "node-tracker",
  "version": "1.0.0",
  "description": "Application de tracking d'informations - PWA",
  "main": "server.js",
  "scripts": {
    "dev": "NODE_ENV=dev nodemon server.js",
    "dev:win": "set NODE_ENV=dev && nodemon server.js",
    "prod": "NODE_ENV=prod node server.js",
    "prod:win": "set NODE_ENV=prod && node server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  }
}
